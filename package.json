{
  "name": "acerbox-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:backend": "node --test tests/backend.test.mjs",
    "test:browser": "node tests/browser.mjs",
    "test:build": "node --test tests/build-smoke.mjs",
    "test:email-draft": "node --test tests/booking-email.test.mjs"
  },
  "dependencies": {
    "gsap": "^3.13.0"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
